# Variáveis de ambiente para o Frontend (React/Vite)

# URL do Backend
# Em desenvolvimento local, use: http://localhost:8000/api/v1
# Na nuvem (Vercel + Render), altere para a URL real do seu backend no Render
VITE_API_URL=http://localhost:8000/api/v1
