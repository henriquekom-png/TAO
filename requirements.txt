# ============================================================
# TAO App — Dependências Python (raiz)
# Uso: scripts utilitários (seed_questoes.py, etc.)
# Para o backend FastAPI, veja backend/requirements.txt
# ============================================================

# ── Banco de dados ────────────────────────────────────────────
psycopg2-binary>=2.9.9      # conexão direta ao PostgreSQL do Supabase
python-dotenv>=1.0.0        # seed_questoes.py: SUPABASE_DB_URL em .env na raiz

# ── AI / Embeddings (usado por scripts utilitários) ──────────
google-generativeai>=0.8.0  # Gemini SDK
google-genai>=1.0.0         # Gemini SDK novo

# ── Vector store (se usado diretamente por scripts) ──────────
chromadb>=0.5.0
sentence-transformers>=2.6.0
protobuf>=3.20.3,<5.0.0     # pin para compatibilidade com chromadb

# ── FSRS ─────────────────────────────────────────────────────
fsrs>=6.3.0                 # algoritmo Free Spaced Repetition Scheduler v5
