# ============================================================
# TAO App — Dependências Python
# Instalar: pip install -r requirements.txt
# ============================================================

# ── Sprint 1+2: Base, banco de dados e interface ─────────────
streamlit>=1.33.0           # Framework web + st.fragment (rerun parcial)

# ── Sprint 3+4: Visualizador e anotações (preparação) ────────
# (sem novas dependências — apenas stdlib + streamlit)

# ── Sprint 5: Portais e menu de contexto ─────────────────────
# streamlit-js-eval>=0.1.7  # (descomente no Sprint 5)

# ── Sprint 6: Upload, embedding e ChromaDB ───────────────────
chromadb>=0.5.0
sentence-transformers>=2.6.0
protobuf>=3.20.3,<5.0.0         # pin para compatibilidade com chromadb no Python 3.12
PyMuPDF>=1.23.0           # fitz — leitura de PDFs
python-docx>=1.1.0        # leitura de DOCX

# ── Sprint 7: Chatbot RAG + exportação ───────────────────────
google-generativeai>=0.8.0  # Gemini — SDK usado pelo chatbot (google.generativeai)
google-genai>=1.0.0         # Gemini — SDK novo (reservado para futuro)
openai>=1.30.0              # OpenAI — gpt-4o-mini
Jinja2>=3.1.3               # templates HTML de exportação
# pdfkit>=1.0.0             # PDF direto (requer wkhtmltopdf no sistema)

# ── Sprint 8: Supabase / PostgreSQL ──────────────────────────
psycopg2-binary>=2.9.9      # conexão direta ao PostgreSQL do Supabase

# ── Fast Entry Telegram (opcional; secrets TELEGRAM_* + SUPABASE_DB_URL) ──
python-telegram-bot>=21.0

# ── Sprint 9: FSRS v5 + exportação PDF real ──────────────────
fsrs>=6.3.0                 # algoritmo Free Spaced Repetition Scheduler v5
xhtml2pdf>=0.2.17           # motor PDF puro-Python (HTML → PDF via ReportLab)
